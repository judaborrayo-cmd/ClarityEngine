Please update the How We Build Faster slider for a tighter, centered layout (keep content and continuous scroll):

Constrain the slider frame to max-w-5xl mx-auto so it matches other sections.

Show 3 full cards on desktop, 2 on tablet, 1 on mobile. Use fixed card bases with Tailwind arbitrary values:

mobile: basis-[100%]

md: basis-[calc((100%-24px)/2)] with mr-6

lg: basis-[calc((100%-48px)/3)] with mr-6

Keep continuous left/right wrap; arrows should jump from start → end and end → start.

Arrows: center vertically, symmetrical horizontally, always visible on desktop, 32px circle with bg-purple-600 text-white, hover bg-purple-700.

Keep the larger “powered-by” chips.

Slightly increase the center title readability: text-base on md+, leading-snug.

Replace src/components/BuildFaster.jsx with the code below (no other changes).

Code (replace src/components/BuildFaster.jsx)
// src/components/BuildFaster.jsx
import React, { useMemo, useRef } from "react";

/**
 * Images expected in /public/images/ (adjust if your names differ):
 * chatgpt.png, Midjourney-Logo.png, Replit_logo.png, vmake.png,
 * Photoshop.png, Figma-Logo.png, YouTube-logo.png,
 * Google-Ads-Logo.jpg, Google-Analytics.png, Shopify-Logo.png,
 * Google-Drive-Logo.png, Dropbox-Logo.png, Microsoft-Teams.png,
 * gemini-logo.png, Reddit-Logo.png, Instagram-Logo.png
 */

type Mini = { name: string; img: string };
type Item = {
  type: "tool" | "solution";
  name: string;         // for top logo alt
  centerLabel: string;  // bold center title
  desc: string;
  main?: { img?: string; emoji?: string };
  stack?: Mini[];
};

const ITEMS: Item[] = [
  // Tools (center labels per your request)
  {
    type: "tool",
    name: "ChatGPT",
    centerLabel: "Strategy and Execution",
    desc: "Strategy generation & prompt engineering",
    main: { img: "/images/chatgpt.png" },
    stack: [
      { name: "Replit", img: "/images/Replit_logo.png" },
      { name: "Gemini", img: "/images/gemini-logo.png" },
    ],
  },
  {
    type: "tool",
    name: "MidJourney",
    centerLabel: "Visuals",
    desc: "Concept visuals & brand imagery",
    main: { img: "/images/Midjourney-Logo.png" },
    stack: [
      { name: "Photoshop", img: "/images/Photoshop.png" },
      { name: "Figma", img: "/images/Figma-Logo.png" },
    ],
  },
  {
    type: "tool",
    name: "Replit",
    centerLabel: "Landing Pages",
    desc: "Rapid code & web builds",
    main: { img: "/images/Replit_logo.png" },
    stack: [
      { name: "Replit", img: "/images/Replit_logo.png" },
      { name: "YouTube", img: "/images/YouTube-logo.png" },
    ],
  },
  {
    type: "tool",
    name: "VMake",
    centerLabel: "Visual Enhancements",
    desc: "AI image enhancement & upscaling",
    main: { img: "/images/vmake.png" },
    stack: [
      { name: "Photoshop", img: "/images/Photoshop.png" },
      { name: "MidJourney", img: "/images/Midjourney-Logo.png" },
    ],
  },

  // Solutions
  {
    type: "solution",
    name: "Conversion Rate Optimization",
    centerLabel: "Conversion Rate Optimization",
    desc: "Friction fixes, proof, speed, clearer CTAs.",
    main: { emoji: "⚡️" },
    stack: [
      { name: "Google Ads", img: "/images/Google-Ads-Logo.jpg" },
      { name: "GA4", img: "/images/Google-Analytics.png" },
      { name: "Shopify", img: "/images/Shopify-Logo.png" },
    ],
  },
  {
    type: "solution",
    name: "Content Creation",
    centerLabel: "Content Creation",
    desc: "On-brand assets for ads, social, landing pages.",
    main: { emoji: "✍️" },
    stack: [
      { name: "Canva", img: "/images/Photoshop.png" /* TODO: swap to Canva path if added */ },
      { name: "MidJourney", img: "/images/Midjourney-Logo.png" },
      { name: "Instagram", img: "/images/Instagram-Logo.png" },
      { name: "YouTube", img: "/images/YouTube-logo.png" },
    ],
  },
  {
    type: "solution",
    name: "Collaboration & Workflows",
    centerLabel: "Collaboration & Workflows",
    desc: "Tight feedback loops with async + weekly cadence.",
    main: { emoji: "🤝" },
    stack: [
      { name: "Teams", img: "/images/Microsoft-Teams.png" },
      { name: "Drive", img: "/images/Google-Drive-Logo.png" },
      { name: "Dropbox", img: "/images/Dropbox-Logo.png" },
    ],
  },
  {
    type: "solution",
    name: "Ideation & Strategy",
    centerLabel: "Ideation & Strategy",
    desc: "Offer angles, testing plans, priorities.",
    main: { emoji: "💡" },
    stack: [
      { name: "MidJourney", img: "/images/Midjourney-Logo.png" },
      { name: "Replit", img: "/images/Replit_logo.png" },
      { name: "Figma", img: "/images/Figma-Logo.png" },
      { name: "ChatGPT", img: "/images/chatgpt.png" },
    ],
  },
  {
    type: "solution",
    name: "Performance Analysis",
    centerLabel: "Performance Analysis",
    desc: "Benchmarks, cohorts, ROI clarity.",
    main: { emoji: "📊" },
    stack: [
      { name: "GA4", img: "/images/Google-Analytics.png" },
      { name: "ChatGPT", img: "/images/chatgpt.png" },
      { name: "Gemini", img: "/images/gemini-logo.png" },
    ],
  },
];

// Larger chip (logo + label)
function StackChip({ name, img }: Mini) {
  return (
    <div className="inline-flex items-center gap-2 rounded-full border border-gray-200 bg-white px-3.5 py-1.5 text-[13px] leading-none shadow-sm hover:shadow transition">
      <img src={img} alt={name} className="h-[28px] w-[28px] object-contain" />
      <span className="text-gray-800">{name}</span>
    </div>
  );
}

function Card({ item }: { item: Item }) {
  return (
    <div
      className="
        shrink-0 basis-[100%] mr-6 snap-start
        md:basis-[calc((100%-24px)/2)]
        lg:basis-[calc((100%-48px)/3)]
      "
      data-card
    >
      <div className="h-full rounded-2xl border border-gray-200 bg-white shadow-sm p-5 text-center">
        {item.main?.img ? (
          <img src={item.main.img} alt={item.name} className="h-12 w-auto mx-auto mb-2 object-contain" />
        ) : (
          <div className="h-12 w-12 mx-auto mb-2 rounded-xl bg-gray-50 border border-gray-200 flex items-center justify-center">
            <span className="text-xl">{item.main?.emoji ?? "★"}</span>
          </div>
        )}

        <h3 className="font-semibold text-gray-900 text-sm md:text-base leading-snug">{item.centerLabel}</h3>
        <p className="text-[12px] text-gray-600 leading-snug mt-1">{item.desc}</p>

        {item.stack && item.stack.length > 0 && (
          <div className="mt-3 flex flex-wrap justify-center gap-2 max-w-[360px] mx-auto">
            {item.stack.slice(0, 4).map((m) => (
              <StackChip key={m.name} name={m.name} img={m.img} />
            ))}
            {item.stack.length > 4 && (
              <div className="inline-flex items-center gap-2 rounded-full border border-gray-200 bg-white px-3.5 py-1.5 text-[13px] leading-none shadow-sm">
                +{item.stack.length - 4}
              </div>
            )}
          </div>
        )}
      </div>
    </div>
  );
}

export default function BuildFaster() {
  const trackRef = useRef<HTMLDivElement>(null);
  const items = useMemo(() => ITEMS, []);

  // width of one card (first child) + 24px gap (mr-6)
  const cardWidth = () => {
    const el = trackRef.current;
    if (!el) return 0;
    const first = el.querySelector<HTMLElement>("[data-card]");
    return first ? first.offsetWidth + 24 : el.clientWidth * 0.9;
  };

  const scrollPrev = () => {
    const el = trackRef.current;
    if (!el) return;
    if (Math.round(el.scrollLeft) <= 0) {
      el.scrollTo({ left: el.scrollWidth - el.clientWidth, behavior: "smooth" });
      return;
    }
    el.scrollBy({ left: -cardWidth(), behavior: "smooth" });
  };

  const scrollNext = () => {
    const el = trackRef.current;
    if (!el) return;
    const atEnd = Math.ceil(el.scrollLeft + el.clientWidth) >= el.scrollWidth;
    if (atEnd) {
      el.scrollTo({ left: 0, behavior: "smooth" });
      return;
    }
    el.scrollBy({ left: cardWidth(), behavior: "smooth" });
  };

  return (
    <section className="px-4 py-14" aria-label="How We Build Faster">
      <div className="max-w-5xl mx-auto text-center">
        <h2 className="text-2xl sm:text-3xl font-bold mb-2">How We Build Faster</h2>
        <p className="text-gray-600 max-w-2xl mx-auto mb-6 text-sm sm:text-base">
          We're a strategic partner (fractional CMO energy) blending human insight with AI-accelerated execution — so you get strategy, design, and delivery at 10× speed.
        </p>
      </div>

      <div className="relative max-w-5xl mx-auto">
        {/* Arrows: smaller, centered, symmetrical */}
        <button
          type="button"
          onClick={scrollPrev}
          aria-label="Previous"
          className="hidden lg:flex absolute -left-5 top-1/2 -translate-y-1/2 z-10 h-8 w-8 items-center justify-center rounded-full bg-purple-600 text-white shadow hover:bg-purple-700"
        >
          ‹
        </button>
        <button
          type="button"
          onClick={scrollNext}
          aria-label="Next"
          className="hidden lg:flex absolute -right-5 top-1/2 -translate-y-1/2 z-10 h-8 w-8 items-center justify-center rounded-full bg-purple-600 text-white shadow hover:bg-purple-700"
        >
          ›
        </button>

        {/* Track */}
        <div
          ref={trackRef}
          className="overflow-x-auto scrollbar-hide flex snap-x snap-mandatory"
          style={{ scrollBehavior: "smooth" }}
        >
          {/* small left spacer to soften edge */}
          <div className="shrink-0 basis-[8px]" aria-hidden />
          {items.map((item) => (
            <Card key={item.name} item={item} />
          ))}
          {/* right spacer */}
          <div className="shrink-0 basis-[8px]" aria-hidden />
        </div>
      </div>

      <style>{`
        .scrollbar-hide::-webkit-scrollbar { display: none; }
        .scrollbar-hide { -ms-overflow-style: none; scrollbar-width: none; }
      `}</style>
    </section>
  );
}