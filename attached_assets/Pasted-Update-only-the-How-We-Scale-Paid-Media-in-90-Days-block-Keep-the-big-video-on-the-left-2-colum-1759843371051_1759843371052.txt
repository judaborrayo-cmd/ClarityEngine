Update only the “How We Scale Paid Media in 90 Days” block:

Keep the big video on the left (2 columns wide) with the CTA strip below it; no “watch on YouTube/clip” links.

On the right, replace the current “How We Scale…” mini-graphic with a Month 1 / Month 2 / Month 3+ roadmap card that uses colored accents (purple/green) and concise rows.

Keep the “Audit Teardown (What We Check)” card, but add a Competitor Research row.

Improve visual balance by:

Making the right column a stack of two cards of compact height,

Using items-start and consistent internal spacing, and

Limiting each card’s text with short rows (no long paragraphs).

Do not change other sections. Replace the existing component file with the code below.

Code (replace src/components/ScaleAndAudit.jsx)
// src/components/ScaleAndAudit.jsx
import React from "react";
import { Link } from "react-router-dom";

/** Month-by-month colored roadmap */
function MonthRoadmap() {
  const rows = [
    {
      m: "Month 1 — Discover & Diagnose",
      pts: [
        "Kickoff, goals, KPIs, constraints",
        "Tracking/GTM/GA4 sanity check & fixes",
        "Account audit + quick-win experiments",
        "Offer & creative hypothesis backlog",
      ],
      dot: "bg-purple-600",
      border: "border-purple-200",
    },
    {
      m: "Month 2 — Design & Deploy",
      pts: [
        "New creatives/angles → launch sets",
        "Audience/signals consolidation",
        "Landing-page CRO fixes & speed passes",
        "Budget re-allocation toward early winners",
      ],
      dot: "bg-green-600",
      border: "border-green-200",
    },
    {
      m: "Month 3+ — Drive & Scale",
      pts: [
        "Iterate winners, cut losers",
        "Bidding/delivery tuning, LTV cohorts",
        "Systemized testing cadence (weekly)",
        "New channels/offers while protecting ROAS",
      ],
      dot: "bg-indigo-600",
      border: "border-indigo-200",
    },
  ];

  return (
    <div className="rounded-2xl border border-gray-200 bg-white p-4">
      <div className="text-base font-semibold text-gray-900 mb-1">How We Scale in 90 Days</div>
      <div className="text-[13px] text-gray-600 mb-3">
        A simple month-by-month plan we adapt to your brand.
      </div>

      <ul className="space-y-3">
        {rows.map((r) => (
          <li
            key={r.m}
            className={`rounded-xl border ${r.border} p-3`}
          >
            <div className="flex items-start gap-2">
              <span className={`mt-1 w-2.5 h-2.5 rounded-full ${r.dot}`} />
              <div>
                <div className="text-sm font-medium text-gray-900">{r.m}</div>
                <ul className="mt-1 text-[12px] text-gray-700 grid grid-cols-1 gap-1">
                  {r.pts.map((p) => (
                    <li key={p} className="flex items-start gap-1">
                      <span className="mt-[7px] w-1.5 h-1.5 rounded-full bg-gray-300" />
                      <span>{p}</span>
                    </li>
                  ))}
                </ul>
              </div>
            </div>
          </li>
        ))}
      </ul>
    </div>
  );
}

/** Audit checklist with compact divided rows */
function TeardownList() {
  const items: Array<[string, string]> = [
    ["Tracking & Attribution", "Pixels, CAPI, GTM, events, GA4 hygiene"],
    ["Account Structure", "Objectives, naming, signal quality, consolidation"],
    ["Budget & Pacing", "Spend allocation, phase stability, throttling risks"],
    ["Audiences & Exclusions", "Signals by stage, overlap control, volume"],
    ["Creative Diagnostics", "Hooks, formats, thumb-stop rate, offer angle"],
    ["Competitor Research", "Spy tools, positioning gaps, ad angle library"], // <— added
    ["Bidding & Delivery", "Caps, optimization events, placements, learning"],
    ["Landing Pages & CRO", "Speed, clarity, proof, CTAs, UX frictions"],
    ["Measurement & Benchmarks", "Targets, cohorts, rolling trendlines"],
    ["Quick Wins & 90-Day Plan", "Prioritized roadmap with owners & deadlines"],
  ];

  return (
    <div className="rounded-2xl border border-gray-200 bg-white p-4">
      <div className="text-base font-semibold text-gray-900 mb-1">
        Audit Teardown (What We Check)
      </div>
      <div className="text-[13px] text-gray-600 mb-3">
        The core areas we evaluate to unlock fast wins and guide your 90-day plan.
      </div>
      <ul className="text-sm text-gray-800 divide-y divide-gray-200 rounded-lg border border-gray-100">
        {items.map(([title, sub]) => (
          <li key={title} className="py-2 px-3">
            <div className="font-medium text-gray-900">{title}</div>
            <div className="text-[12px] text-gray-600">{sub}</div>
          </li>
        ))}
      </ul>
    </div>
  );
}

export default function ScaleAndAudit({
  videoUrl = "https://www.youtube.com/embed/dQw4w9WgXcQ",
}) {
  return (
    <section className="container mx-auto px-4 py-12" aria-label="Scale in 90 Days">
      <h2 className="text-2xl sm:text-3xl font-bold mb-2">How We Scale Paid Media in 90 Days</h2>
      <p className="text-gray-600 mb-6">
        Discovery → Diagnose → Design → Drive — applied through a month-by-month plan.
      </p>

      <div className="grid lg:grid-cols-3 gap-6 items-start">
        {/* Left: Video (2 cols) */}
        <div className="lg:col-span-2">
          <div className="relative w-full overflow-hidden rounded-2xl shadow-sm border border-gray-200">
            <div className="w-full h-0 pb-[56.25%] relative">
              <iframe
                className="absolute inset-0 w-full h-full"
                src={videoUrl}
                title="How We Scale in 90 Days"
                frameBorder="0"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                allowFullScreen
              />
            </div>
          </div>

          {/* Primary CTA under video */}
          <div className="mt-5 rounded-2xl border border-gray-200 bg-white p-4 sm:flex sm:items-center sm:justify-between">
            <div className="text-sm">
              <div className="font-semibold text-gray-900">
                Get an Actionable Roadmap in 2–3 Weeks
              </div>
              <div className="text-gray-600 text-xs">
                Written 20–30 page audit + 90-day plan, then a live walkthrough.
              </div>
            </div>
            <Link
              to="/checkout/audit"
              className="mt-3 sm:mt-0 inline-flex items-center justify-center rounded-xl bg-green-500 text-white hover:bg-green-600 text-sm py-2 px-4 font-medium"
            >
              Order Your Deep Dive Audit
            </Link>
          </div>
        </div>

        {/* Right: Two compact cards */}
        <div className="space-y-4">
          <MonthRoadmap />
          <TeardownList />
        </div>
      </div>
    </section>
  );
}